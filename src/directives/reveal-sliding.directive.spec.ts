import { RevealSlidingDirective } from './reveal-sliding.directive';

describe('RevealSlidingDirective', () => {
  it('should create an instance', () => {
    const directive = new RevealSlidingDirective();
    expect(directive).toBeTruthy();
  });
});
